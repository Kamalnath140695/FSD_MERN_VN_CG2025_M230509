const Q7C = (props) => {
    return (
        <div>
            <h3>Child Component (Q7C)</h3>
            <button onClick={props.onButtonClick}>
                Click to Show Alert
            </button>
        </div>
    );
};

export default Q7C;